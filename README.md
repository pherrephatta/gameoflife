# Game Of Life

This branch is set up to work and compile on Linux.
The header files were changed from `.h` to `.hpp` to specify that C++ is used, and not C.
The code itself is unchanged.
The makefile facilitates compilation with SDL.
